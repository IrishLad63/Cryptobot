{
  "name": "cryptobot-vite-app",
  "version": "1.0.0",
  "description": "A Vite app with a custom Node (Express) server for Fly.io deployment.",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
